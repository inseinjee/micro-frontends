{
    "name": "cart",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "babel-cli": "^6.24.0",
        "react": "^15.4.2",
        "react-dom": "^15.4.2"
    },
    "devDependencies": {
        "babel-preset-env": "^1.6.1",
        "react-scripts": "0.9.5"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "set public_url=http://localhost:8083&&react-scripts build&&node server.js",
        "test": "react-scripts test --env=jsdom",
        "eject": "react-scripts eject",
        "transpile": "set NODE_ENV=production && babel src --out-dir transpiled",
        "start:prod": "set NODE_ENV=production && node server.js"
    }
}